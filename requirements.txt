# For MCP Server mode (optional, only needed if using mcp_server.py)
mcp>=1.0.0

# No other third-party Python packages required.
# The GUI and CLI modes use only the Python standard library + FFmpeg (system dependency).
#
# To install FFmpeg:
#   Windows: https://ffmpeg.org/download.html  (add to PATH)
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg
